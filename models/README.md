# Models

The trained model files are not included in this public repository.

The project uses:
- Random Forest
- Bidirectional LSTM
- Hybrid decision fusion

Model training and implementation details are documented separately.
